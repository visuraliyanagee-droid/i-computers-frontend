import UserData from "./userData";

export default function Header(){
    return(
        <header>
            <div>
        <h1>My shop</h1>
        <span>cart(0)</span>
        <p>welcome to shop</p>
            </div>
        </header>

    )
}